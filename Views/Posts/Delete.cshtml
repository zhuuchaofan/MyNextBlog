@model MyTechBlog.Models.Post

@{
    ViewData["Title"] = "删除确认";
}

<div class="row justify-content-center mt-5">
    <div class="col-md-6">
        <div class="card border-0 shadow-lg">
            <div class="card-header bg-danger text-white text-center py-3">
                <h4 class="mb-0 fw-bold">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i> 删除确认
                </h4>
            </div>
            
            <div class="card-body p-5 text-center">
                <p class="lead mb-4">你确定要永久删除这篇文章吗？<br><span class="text-muted fs-6">此操作无法撤销。</span></p>
                
                <div class="alert alert-light border mb-4">
                    <h5 class="fw-bold text-dark mb-2">@Model.Title</h5>
                    <small class="text-muted">发布于: @Model.CreateTime.ToString("yyyy-MM-dd HH:mm")</small>
                </div>

                <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    
                    <div class="d-grid gap-2">
                        <input type="submit" value="确认删除" class="btn btn-danger btn-lg" />
                        <a asp-action="Index" class="btn btn-outline-secondary">取消，留着它</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>